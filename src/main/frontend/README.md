# Frontend 안내

이 디렉터리는 SmileTrack 프론트엔드(React) 코드입니다.

- 제출용 프로젝트 설명과 실행 순서는 루트 [README.md](../../README.md)를 기준으로 확인합니다.
- 프론트 실행 위치는 `src/main/frontend`입니다.
- 개발 서버 기본 포트는 `3000`이며, `package.json`의 proxy는 `http://localhost:8080`입니다.

실행:

```bash
npm install
npm start
```
