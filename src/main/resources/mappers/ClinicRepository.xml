<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.smiletrack.repository.ClinicRepository">
    <insert id="insertClinic" parameterType="com.example.smiletrack.vo.Clinic" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO clinic (clinic_name, alias, phone, email, address, shipping_notes, account_manager_id)
        VALUES (#{clinicName}, #{alias}, #{phone}, #{email}, #{address}, #{shippingNotes}, #{accountManagerId})
    </insert>
</mapper>